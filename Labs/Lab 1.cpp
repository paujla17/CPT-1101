#include <iostream>

using namespace std; 

int main() { 
    int a, b, c, d; 

    cin>> a>> b>> c; 

    if (a == 0) {
        cout<< "display, not quadratic";
        if (b == 0) {
            cout<<"a=0, b=0 bye"; 
        
        else{ 
            x1= -1* c/b
            cout<<"x1" <<x1 << endl; 
        }
        }
      
        
        ;
    
    }
    else {
        d= b*b-4*a*c
    }
        if (d <0) {
        cout<< "no real answer" <<no real answer <<endl
    }
        else {x1= -1 * b + sqrt(d) / 2 * a 
        x2= -1 * b - sqrt(d) / 2 * a }

        cout<<"x1" << x1 << endl; 
        cout<<"x2" << x2 << endl; 
}